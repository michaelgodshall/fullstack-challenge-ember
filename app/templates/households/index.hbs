<div>
  <div>
    <h1>Households</h1>
  </div>
  <ul class="list-group">
    {{#each model as |household|}}
      <li class="list-group-item list-group-item-action">
        {{#link-to 'households.show' household.id}}{{household.fullAddress}}{{/link-to}}
      </li>
    {{/each}}
  </ul>
</div>
{{outlet}}
